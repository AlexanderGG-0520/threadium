package dev.alex.threadium.render.modelpart.material;

/** Explicit diagnostic source supplied by a verified provider observation hook. */
public enum MaterialProviderSource {
    IMMEDIATE,
    OUTLINE,
    IMMEDIATELY_FAST,
    OTHER_VERIFIED,
    UNAVAILABLE
}
