name: CI

on:
  push:
    branches:
      - mc1.21.1/fabric/dev
  pull_request:
    branches:
      - mc1.21.1/fabric/dev
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java 25
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: "25"

      - name: Set up Gradle 9.6.1
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.6.1"

      - name: Validate Java 21 backport
        run: gradle --no-daemon clean spotlessApply spotlessCheck test build

      - name: Collect 26.2 reference sources
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p build/reference-26.2
          git fetch --no-tags --depth=1 origin mc26.2/fabric/dev
          git archive origin/mc26.2/fabric/dev \
            src/client/java/dev/alex/threadium/render/modelpart \
            src/client/java/dev/alex/threadium/config \
            src/client/java/dev/alex/threadium/metrics \
            src/client/java/dev/alex/threadium/benchmark \
            src/client/java/dev/alex/threadium/compat \
            src/client/java/dev/alex/threadium/mixin \
            src/client/resources \
            src/test/java/dev/alex/threadium/render/modelpart \
            src/test/java/dev/alex/threadium/config \
            src/test/java/dev/alex/threadium/benchmark \
            | tar -x -C build/reference-26.2

      - name: Upload validation workspace
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mc1211-validation-${{ github.sha }}
          if-no-files-found: warn
          retention-days: 3
          path: |
            src/backport1211
            build.gradle
            gradle.properties
            settings.gradle
            gradle
            gradlew
            gradlew.bat
            build/reference-26.2
            build/reports
            build/test-results
            build/libs
